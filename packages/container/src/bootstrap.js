import React from 'react';
import ReactDOM from 'react-dom';
import App from './app';


//no munt function neede because we want it to appear immediately on the screen
ReactDOM.render(<App />, document.querySelector('#root'));